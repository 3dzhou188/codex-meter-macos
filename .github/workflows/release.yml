name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing version tag to publish
        required: true
        default: v3.1.0

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ env.RELEASE_TAG }}
      - name: Build app
        run: ./scripts/build-app.sh
      - name: Package app
        run: |
          version="$RELEASE_TAG"
          version="${version#v}"
          archive="dist/Codex-Meter-macOS-arm64-${version}.zip"
          ditto -c -k --sequesterRsrc --keepParent "dist/Codex Meter.app" "$archive"
          shasum -a 256 "$archive" | tee dist/SHA256SUMS.txt
      - name: Write release notes
        run: |
          cat > /tmp/release-notes.md <<'NOTES'
          ## 中文

          Codex Meter 3.1.0 修复 Codex 官方暂时取消 5 小时用量限制后出现的显示 bug。当前只返回 1 周用量时，App 会将 5 小时额度显示为“暂无限制”，同时继续准确显示真实的 7 天额度；官方恢复两档限制后也会自动恢复正常测量显示。

          本版本同时修复 Agent 状态灯经常显示“状态不可信”的问题，校正会话过期、失败判断和红黄绿状态转换逻辑。

          本版本兼容新版 ChatGPT 桌面应用。OpenAI 将 Codex 桌面应用整合并更名为 ChatGPT 后，App 可从 `/Applications/ChatGPT.app` 自动发现内置 Codex 实时服务，同时继续兼容旧版 Codex.app 和 Homebrew Codex CLI。

          “打开 Codex”操作也会优先打开 ChatGPT，并在未安装新版应用时回退到旧版 Codex。

          本版本还重新设计了应用图标，采用单层米色 macOS 圆角底板，保留终端、额度弧线和三色状态灯三个核心元素。

          额度数据仍只来自 Codex 实时服务；`~/.codex/sessions` 仅用于读取 Codex 活动事件，明确忽略 `token_count`，不会读取 `~/.codex/auth.json`。

          Codex Agent 状态灯功能参考并受启发于 [Agent Signal Bar](https://github.com/guan-ops/Agent-Signal-Bar) 的本地优先 AI Agent 三色信号灯设计。Codex Meter 中的实现限定为 Codex 场景，并按本项目代码结构重新实现；不包含 Agent Signal Bar 的图片、声音、图标、截图、品牌素材或应用资源。

          下载 ZIP，解压后将应用移入“应用程序”文件夹。当前发布包采用临时签名，首次运行可能需要右键应用并选择“打开”。如果 macOS 仍提示“无法打开”或“已损坏”，可执行：

          ```bash
          xattr -dr com.apple.quarantine "/Applications/Codex Meter.app"
          open "/Applications/Codex Meter.app"
          ```

          这是因为当前开源发布包尚未使用 Apple Developer ID 签名并公证。

          ## English

          Codex Meter 3.1.0 fixes the usage display bug caused by Codex temporarily removing the five-hour limit. When Codex returns only the weekly window, the app shows the five-hour module as “No limit” while continuing to display the real seven-day usage; normal two-window measurement resumes automatically when the official five-hour limit returns.

          This release also fixes false “Status unavailable” Agent light states by correcting session expiry handling, failure detection, and red/yellow/green state transitions.

          The release supports the renamed ChatGPT desktop app. After OpenAI integrated the Codex desktop experience into ChatGPT, this release can discover the bundled Codex realtime service at `/Applications/ChatGPT.app`, while retaining compatibility with the legacy Codex.app and Homebrew Codex CLI installations.

          The “Open Codex” action now opens ChatGPT first and falls back to the legacy Codex app when needed.

          This release also introduces a redesigned app icon with a single warm ivory macOS-style rounded tile while preserving the terminal mark, usage arc, and traffic-light status indicators.

          Usage data still comes only from the Codex realtime service. `~/.codex/sessions` is read only for Codex activity events, `token_count` is explicitly ignored, and `~/.codex/auth.json` is not read.

          The Codex Agent status light feature is inspired by [Agent Signal Bar](https://github.com/guan-ops/Agent-Signal-Bar) and its local-first red/yellow/green signal-light design for AI agents. Codex Meter's implementation is scoped to Codex and reimplemented in this project's own structure; it does not include Agent Signal Bar images, sounds, icons, screenshots, brand assets, or app resources.

          Download the ZIP, extract it, and move the app to Applications. Release builds are currently ad-hoc signed. On first launch, you may need to right-click the app and choose **Open**. If macOS still says the app cannot be opened or is damaged, run:

          ```bash
          xattr -dr com.apple.quarantine "/Applications/Codex Meter.app"
          open "/Applications/Codex Meter.app"
          ```

          This is required because the current open-source release is not yet signed with an Apple Developer ID certificate and notarized.
          NOTES
      - name: Publish GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Codex Meter ${{ env.RELEASE_TAG }}
          body_path: /tmp/release-notes.md
          files: |
            dist/Codex-Meter-macOS-arm64-*.zip
            dist/SHA256SUMS.txt
